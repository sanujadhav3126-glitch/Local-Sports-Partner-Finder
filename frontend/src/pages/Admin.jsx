import { useEffect,useState } from 'react'; import api from '../api/api';
export default function Admin(){ const [stats,setStats]=useState(null); const [users,setUsers]=useState([]); const load=async()=>{try{setStats((await api.get('/admin/stats')).data); setUsers((await api.get('/admin/users')).data)}catch(e){setStats({error:'Admin access required'})}}; useEffect(()=>{load()},[]);
if(stats?.error) return <div className="container"><h2>{stats.error}</h2></div>;
return <div className="container"><h2>Admin Dashboard</h2>{stats&&<div className="grid"><div className="dash">Users: {stats.users}</div><div className="dash">Games: {stats.games}</div><div className="dash">Requests: {stats.requests}</div><div className="dash">Successful Matches: {stats.successfulMatches}</div></div>}<h3>Users</h3><table><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>City</th></tr></thead><tbody>{users.map(u=><tr key={u._id}><td>{u.name}</td><td>{u.email}</td><td>{u.role}</td><td>{u.location?.city}</td></tr>)}</tbody></table></div> }
